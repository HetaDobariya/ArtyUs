import SPInfo from "@/app/modules/dashboard/sp/spinfo";

const page = () => {
    return (
        <div>
            {/* Change this to the imported component name */}
            <SPInfo />
        </div>
    );
};

export default page;