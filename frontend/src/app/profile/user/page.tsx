// Change this line to import the correct component name (UserInfo)
import UserInfo from '../../modules/dashboard/user/UserInfo'; 

const page = () => {
  return (
    <div>
      {/* Change this to the imported component name */}
      <UserInfo /> 
    </div>
  );
};

export default page;